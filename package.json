{
  "name": "watchtogether",
  "version": "1.0.0",
  "description": "Watch movies in perfect sync with a friend",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && node index.js",
    "install:all": "cd client && npm install && cd ../server && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
