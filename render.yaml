services:
  - type: web
    name: watchtogether
    runtime: node
    buildCommand: cd client && npm install && npm run build
    startCommand: cd server && node index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
